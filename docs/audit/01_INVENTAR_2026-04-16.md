# Inventar - 2026-04-16

## Gesamtbild
1. Gesamtordner inkl. lokaler Umgebung: `10285` Dateien.
2. Dominanter Aufblaehfaktor: `.venv` (lokale Virtualenv im Projektordner).
3. Keine Jupyter-Notebooks gefunden (`*.ipynb` = 0).

## Struktur nach Bereichen (inhaltlich)
1. `app/`  
   Produktivcode (FastAPI, Router, Services, Templates, Static, i18n).
2. `alembic/`  
   DB-Migrationen und Schemaentwicklung.
3. `tests/`  
   Unit/Integration/E2E-Tests.
4. `scripts/`  
   Hilfsskripte (Seed/Import/Smoke).
5. `tools/`  
   Diagnose- und Wartungstools.
6. `docs/`  
   Fach-/Technikdokumentation und Checklisten.
7. `diagnostics/`  
   Laufberichte/Pruefartefakte (teilweise versioniert).
8. `backups/`, `outbox/`, lokale DB-Dateien, `.venv/`, `__pycache__/`  
   Arbeits-/Laufartefakte, nicht fuer ein sauberes Public-Repo-Root geeignet.

## Groesse und Umfang (ohne `.git/.venv/.pytest_cache`)
1. `app`: 145 Dateien, ~1.60 MB
2. `tests`: 108 Dateien, ~1.05 MB
3. `alembic`: 52 Dateien, ~0.19 MB
4. `docs`: 14 Dateien, ~0.06 MB
5. `scripts`: 12 Dateien, ~0.06 MB
6. `tools`: 31 Dateien, ~0.35 MB
7. `diagnostics`: 14 Dateien, ~0.06 MB
8. `backups`: 4 Dateien, ~17.30 MB

## Root-Inhalte (Bewertung)
### Wahrscheinlich produktiv relevant
1. `app/`, `alembic/`, `alembic.ini`
2. `requirements.txt`
3. `Dockerfile`, `docker-compose.yml`, `render.yaml`, `start.sh`
4. `.env.example`
5. Kern-README (nach Konsolidierung)

### Wahrscheinlich Altlast/umzuorganisieren
1. `DELIVERABLE_*.md` im Root
2. `README_*.md`-Satelliten im Root
3. `projekt_3_meal_mate.pdf`
4. `rezepte_kochwiki_clean_3713.csv` (nur als separates Sample/Data-Asset oder Download-Referenz)
5. `diagnostics/*` Laufreports
6. lokale Datenbanken (`mealmate.db*`, `tmp_dbg.db`)
7. `.venv/`, `__pycache__/`, `.pytest_cache/`

## Git-Tracking-Beobachtung
1. Getrackte Dateien gesamt: `216`.
2. Root-getrackte Dateien: `37` (fuer ein Public-Produktrepo zu viele fachfremde Root-Dateien).
3. Positiv: `.env`, lokale DBs, `backups/`, `outbox/` sind durch `.gitignore` grundsaetzlich abgedeckt.
4. Negativ: `diagnostics/` Reports sind versioniert und mischen Laufspuren in den Hauptzweig.

## Besondere Inkonsistenzen
1. Produktname driftet zwischen `MealMate` und `Kitchen Hell and Heaven`.
2. `app/services/` (Ordner) steht leer neben `app/services.py` (Datei) und sendet falsche Struktur-Signale.
3. Lokale `.venv` zeigt veraltete externe Pfade und ist nicht reproduzierbar.
